// Used instead of concatentation

const title = "I'm on top of the world";
const artist = 'Imagine Dragons';

let output = `the song called ${title} by ${artist}`;
console.log(output);

// HTML Templates

let html = `
<h1>${title}</h1>
<h2>${artist}</h2>
`

console.log(html);